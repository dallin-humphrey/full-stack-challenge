version: '3'
services:
  my-service:
    container_name: my-container-name
    image: my-image:latest
    ports:
      - "4000:4000"
      - '5173:5173'
    volumes:
      - .:/usr/src/app:cached
    command: ['npm', 'run', 'server:dev']
